"""Turning a connector's raw text into retrievable, embedded chunks — crawling a website
(crawl.py), splitting extracted text into overlapping pieces (chunk.py), both driven by the
background worker (app/workers/index_connector.py)."""

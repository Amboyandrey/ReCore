"""The memory layer (ReMind): recall and recording backed by mem0.ai, offered to assistants that
opt in. See app/memory/mem0.py for the wire adapter and app/services/memory.py for the domain
logic (scoping, authorization, and the chat-pipeline hooks) built on top of it."""
